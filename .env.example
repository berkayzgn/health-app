# Kopyala: cp .env.example .env
# Bilgisayarının yerel IP’si (Wi‑Fi). Terminal: ipconfig getifaddr en0   veya   ifconfig | grep inet
# Backend aynı makinede 3000 portunda çalışırken fiziksel iPhone/Android için zorunlu sayılır.
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
